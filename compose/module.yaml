product:
  type: jvm/app

apply:
  - ../shared/common.module-template.yaml

settings:
  jvm:
    mainClass: MainKt
  compose:
    enabled: true
    experimental:
      hotReload: enabled

dependencies:
  - ../shared
  - $libs.slf4j.simple
  - $compose.desktop.currentOs
