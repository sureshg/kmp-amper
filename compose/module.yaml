product:
  type: jvm/app

apply:
  - ../shared/common.module-template.yaml

settings:
  jvm:
    mainClass: MainKt
  compose:
    version: 1.9.3
    enabled: true
    resources:
      enabled: true
    experimental:
      hotReload:
        version: 1.0.0

dependencies:
  - ../shared
  - $libs.slf4j.simple
  - $compose.desktop.currentOs
