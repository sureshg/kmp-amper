settings:
  jvm:
    release: 21
    storeParameterNames: true

  java:
    freeCompilerArgs:
      - --enable-preview
      - -Xlint:all
    # annotationProcessing:
    #   processors:
    #     - $libs.google.auto.service
    #   processorOptions:
    #     - debug: "true"

test-settings:
  jvm:
    test:
      freeJvmArgs:
        - --enable-preview