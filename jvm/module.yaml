product:
  type: jvm/app

apply:
  - ../shared/common.module-template.yaml

settings:
  jvm:
    mainClass: genai.VoicesKt

dependencies:
  - ../shared
  - $libs.slf4j.simple
  - $libs.voices.chorus
  - $libs.voices.alba
  - $libs.voices.en.us
  - $libs.onnxruntime
