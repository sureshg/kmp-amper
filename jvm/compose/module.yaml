product:
  type: jvm/app

apply:
  - ../../shared/common.module-template.yaml

settings:
  jvm:
    mainClass: MainKt
  compose:
    version: 1.11.0-beta02
    enabled: true
    resources:
      enabled: true
    experimental:
      hotReload:
        version: 1.1.0-rc01

dependencies:
  - ../../shared
  - $libs.slf4j.simple
  - $compose.desktop.currentOs
# - $libs.compose2pdf
